{
  "name": "severka-12-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page SEVERKA 12 — statická single-file stránka (fáze 1)",
  "scripts": {
    "images": "node scripts/build-images.mjs",
    "images:check": "node scripts/build-images.mjs --check",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "sharp": "^0.34.0"
  }
}
